sudo: required

language: php

services:
  - docker

language: php

php:
    - 5.6

before_script:
  - 'npm install'
  - 'composer install'
  - 'chmod -R 777 vendor'

script:
  - 'grunt default'
  - 'chmod -R 777 vendor'
  - 'grunt shell:runTestPrestashop16'
  - 'chmod -R 777 vendor'
  - 'grunt shell:runTestPrestashop17'

